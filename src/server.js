require('dotenv').config(); const express=require('express'); const session=require('express-session'); const passport=require('passport'); const helmet=require('helmet'); const multer=require('multer'); const path=require('node:path'); const {getDb,openDatabase,runMigrations}=require('./db'); const {createSessionStore}=require('./services/session-store'); const {configurePassport}=require('./auth/passport'); const sweeper=require('./services/demo-sweeper');
const auth=require('./routes/auth'),health=require('./routes/health'),workspaces=require('./routes/workspaces');
function createApp(db,opts={}){if(!db){db=getDb();} const app=express();app.set('trust proxy',1);app.locals.db=db; configurePassport(db); const secret=process.env.SESSION_SECRET|| (process.env.NODE_ENV==='production'?null:'development-only-session-secret'); if(!secret)throw new Error('SESSION_SECRET is required in production'); app.use(helmet({contentSecurityPolicy:{directives:{defaultSrc:["'self'"],styleSrc:["'self'"],fontSrc:["'self'"],scriptSrc:["'self'"],imgSrc:["'self'","data:"]}}}));app.use(express.json());app.use(express.urlencoded({extended:true}));const store=createSessionStore(db,opts.sessionStore);app.locals.sessionStore=store;app.use(session({secret,resave:false,saveUninitialized:false,store,cookie:{sameSite:'lax',httpOnly:true,secure:process.env.NODE_ENV==='production'}}));app.use(passport.initialize());app.use(passport.session());app.use(express.static(path.join(__dirname,'../public')));app.use('/api/auth',auth);app.use('/',health);app.use('/api',workspaces);for(const n of ['incidents','entries','search','v1-ingest','techniques','evidence','export','audit','demo'])app.use('/api',require(`./routes/${n}`));if(opts.startSweeper!==false)sweeper.start(db);app.use((error,req,res,next)=>{if(error instanceof multer.MulterError)return res.status(error.code==='LIMIT_FILE_SIZE'?413:400).json({error:error.message});next(error);});app.use('/api',(req,res)=>res.status(404).json({error:'Not found'}));app.use((error,req,res,next)=>{if(res.headersSent)return next(error);res.status(500).json({error:'Internal server error'});});return app;}
if(require.main===module){const db=openDatabase();runMigrations(db);const app=createApp(db);app.listen(process.env.PORT||3059,()=>console.log(`ir-logger listening on ${process.env.PORT||3059}`));}
module.exports={createApp};
