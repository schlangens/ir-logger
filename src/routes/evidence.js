module.exports = require('express').Router();
// mounted at '/api' in src/server.js — declare routes relative to that (e.g. router.get('/incidents/:id', ...)); SPEC.md §5 — filled in by Round 2c (docs/devin-briefs/round2c-evidence-custody.md)
